// const colors = ["red", "green", "blue"];
// 🧠 Task:

// Use forEach() to print each color in uppercase.
const colors = ["red", "green", "blue"];
colors.forEach(color => {
    console.log(color.toUpperCase())
});
