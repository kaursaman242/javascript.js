// const langs = ["HTML", "CSS", "JS"];
// 🧠 Task:

// Use forEach((lang, index) => {...}) to print:
// Language 1: HTML
// Language 2: CSS
// Language 3: JS
const langs = ["HTML", "CSS", "JS"];
langs.forEach((lang, index) => {
    console.log(`Language ${index + 1}: ${lang}`)
});
