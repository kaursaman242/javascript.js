// Create two variables: isEmailCorrect = true, isPasswordCorrect = false.
 let  isEmailCorrect = true
 let isPasswordCorrect = false

// Use the && (AND) operator to check if both are correct.
// Print "Login Successful" if true, otherwise "Login Failed"
isEmailCorrect && isPasswordCorrect ? console.log("Login Successful") : console.log("Login Failed")


