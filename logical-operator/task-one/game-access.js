// Variables: coins = 120, hasPass = false.
   let coins = 120
   let hasPass = false

// A player can access the game if coins >= 100 || hasPass == true.
 coins >= 100 || hasPass == true ? console.log("Access Granted") : console.log( "Access Denied")

// Print "Access Granted" or "Access Denied".