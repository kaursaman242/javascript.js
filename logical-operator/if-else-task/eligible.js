let age = 25;  
let hasDegree = true;  // change to false to test

if (age >= 18 && age <= 30 && hasDegree) {
  console.log("Candidate is eligible");
} else {
  console.log("Candidate is not eligible");
}