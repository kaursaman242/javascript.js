let hasCountryACitizenship = true
let hasCountryBCitizenship = false

if (hasCountryACitizenship && hasCountryBCitizenship) {
  console.log("Person can vote in both countries")
} else {
  console.log("Person cannot vote in both countries")
}