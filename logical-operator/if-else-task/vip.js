let username="progamerX"
let age=5
let issuspended=false
if (username.startsWith("pro") && username.endsWith("X") && age>=2 && !issuspended) {
    console.log("VIP access granted")
}else{
    console.log("access denied")
}